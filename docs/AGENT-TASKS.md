# Coordination

## research next task
Implement ONLY apps/web/src/app/landing.css based on the actual JSX in apps/web/src/components/landing-page.tsx. Root imports this CSS. Build beautiful, distinct, polished full-page responsive RTL designs for .landing-orbit (warm architectural ivory/forest oversized typography panoramic photos), .landing-bloom (wellness cream/sage/dusty rose, arch image and softer editorial layout), and .landing-forma (education dark navy/lime dynamic bold hero). Include all section styles, nav/footer, preview banner, story overlay, mobile. Read JSX selectors and existing shared defaults. Do not change globals.css nor component files. Root owns dashboard globals and frontend logic. Report after file ready.

## backend
Own apps/api + packages/db; complete docs/API-CONTRACT.md using shared types. Local DB is running at DATABASE_URL=postgresql://araland@127.0.0.1:55432/araland. Root .env exists with AUTH_DEV_MODE=true. You may use db generate/push once bun install complete. Include real OTP provider when configured, safe development mode, ownership, draft publication, domain TXT verification, AI env adapter (official docs), image optimization, uploads, private recipient files. Meaningful integration tests against PostgreSQL required. Do not edit root or shared unless coordinated.

## mobile
Own apps/mobile only, Expo SDK57 (official verified React19.2.3/RN0.86.3) + HeroUI Native1.0.9/Uniwind. Match API contract and shared types. Persian RTL login + sites/create + template select + section editing + publish + leads/members + private file uploads and useful content screens. SecureStore session. Avoid web build unsupported by HeroUI. Typecheck and native export where possible. Root installs dependencies. Local server API port4000. Phone uses LAN address via EXPO_PUBLIC_API_URL. Document run and untested device limitations.

## research final UI task
Own only apps/web/src/components/landing-page.tsx and apps/web/src/components/ui.tsx for final fixes. Fix Button loading disabled spread ordering. Accessible Modal and story dialog: focus/return focus, Escape and trap, initial focus, scroll lock. Mobile menu aria-expanded. Active published tagline fix. Derive anchors by section type (for added/replaced sections), and make about CTA honor section properties. Contact href not needed (submit). No root globals edits. Root already fixed safeUrl tel/mailto and will sanitize optional blank URLs in editor; mobile agent owns management-panel fixes and edit forms/posts. After changes run web typecheck.

## mobile final web task
Own only apps/web/src/components/management-panel.tsx. Add edit forms and edit/publish existing posts using new PATCH endpoints. Correct DNS TXT exact value araland-verification=TOKEN displayed/copied. Omit blank cover on creation, null on edit. Fix useLoad cancellation & clear old data while loading. Correct private file limit to 20MB; AI textarea aria-label. Root already fixed its original TS syntax typo, whole web currently passes. Leave CSS alone; existing styles support modal/forms. End typecheck.

## research urgent visual fix
Root CUA inspected Forma at 390x844. Horizontal content is clipped: .landing-hero width390 scrollWidth468, .hero-copy width434 x=-67, h1 width434, .hero-description p width380 x=-13, .hero-photo width430 x=-65. Please fix mobile min-width:0 and grid minmax(0,1fr)/max-width as required in your landing.css (you also own that file). Test other templates mobile if possible. Root observed screenshot and DOM; will visually recheck. Root is using CUA iab browser1 tab1; avoid altering that.

## backend FINAL SECURITY FIX (root requested)
Concrete custom-domain threat found: verified merchant domains can currently request OTP and receive unrestricted global owner sessions; their controlled origin must receive a site-scoped portal session only. Implement optional siteSlug on auth/request and automatic server-derived scope when request Origin is a verified published merchant domain. Add nullable siteScopeId fields on OtpChallenge and Session via incremental migration; challenge scope must determine final session scope and cannot be upgraded/downgraded by verify request origin. Refuse verifying an unscoped manager challenge from a merchant origin; scoped challenge should bind matching site origin or configured app origin/native (no origin). For requests on custom origin also restrict existing unrestricted bearer tokens to origin site (defense in depth). userFrom can return user + scope metadata, private management endpoints (including GET/POST /sites and sitesFor response) must deny scoped sessions, portal/join/files must require matching site scope; scoped sessions do not have owner override on private files. /me may return user+scope; logout works for all. Scoped verify should return sites:[], siteScopeId and/or siteSlug. Local app/native manager login without siteSlug remains existing global flow. Root owns Auth UI: will send siteSlug derived from /portal/:slug returnTo and store token by site to avoid overwriting manager auth. Include tests: owner identity using portal scope cannot list/create/edit own sites; scope cannot read/join/download another site; merchant-origin force scope, unscoped challenge verify cannot be harvested there; management origin unchanged. Update API docs. Do not edit web/mobile/shared beyond optional returned auth fields contract. Preserve createInitialContent, labels and prior tests. Root is doing web token namespacing and docs.
